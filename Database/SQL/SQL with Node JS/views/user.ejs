<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            font-family: Arial, Helvetica, sans-serif;
        }

        table {
            width: 90%;
            border-collapse: collapse;
            margin: 0 auto;
            margin-top: 30px;
            font-size: 18px;
            text-align: center;
        }

        th,
        td {
            padding: 12px;
            border: 1px solid black;
        }
    </style>
</head>

<body>
    <table>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>

        <tr>
            <%for(result of results) {%>
            <td><%=result.id%></td>
            <td><%=result.name%></td>
            <td><%=result.email%></td>
            <td>
                <form action="/user/<%=result.id%>/edit">
                    <button style="background-color: transparent; border: none; font-size: 15px; cursor: pointer; color: green;"><b>Edit user name</b></button>
                </form>
            </td>
            <td>
                <form action="/user/<%=result.id%>/delete">
                    <button style="background-color: transparent; border: none; font-size: 15px; cursor: pointer; color: green;"><b>Delete user name</b></button>
                </form>
            </td>
        </tr>
        <%}%>
        
    </table>
</body>

</html>